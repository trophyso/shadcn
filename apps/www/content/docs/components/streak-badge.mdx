---
title: Streak Badge
description: A compact badge displaying the current streak count with customizable styling.
---

<ComponentPreview name="streak-badge" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.trophy.so/r/streak-badge.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="streak-badge" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

Import the component:

```tsx
import { StreakBadge } from "@/components/ui/streak-badge"
```

Basic usage with a streak length:

<ComponentPreview name="streak-badge-basic" />

## Examples

### Variants

Three visual variants are available: `default`, `outline`, and `ghost`.

<ComponentPreview name="streak-badge-variants" />

### Sizes

Available in three sizes: `sm`, `default`, and `lg`.

<ComponentPreview name="streak-badge-sizes" />

### With Frequency Label

Show the frequency label (e.g., "7 days") alongside the count.

<ComponentPreview name="streak-badge-frequency" />

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `streak` | `StreakResponse` | - | Streak data object |
| `length` | `number` | - | Manual streak length (alternative to streak prop) |
| `variant` | `"default" \| "outline" \| "ghost"` | `"default"` | Visual style variant |
| `size` | `"sm" \| "default" \| "lg"` | `"default"` | Badge size |
| `showFrequency` | `boolean` | `false` | Show frequency label (e.g., "7 days") |
| `showFlame` | `boolean` | `true` | Show the flame icon |
| `icon` | `ReactNode` | - | Custom icon to replace flame |

### Types

```ts
interface StreakResponse {
  length: number
  frequency: "daily" | "weekly" | "monthly"
}
```
