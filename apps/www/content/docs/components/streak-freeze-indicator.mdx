---
title: Streak Freeze Indicator
description: Visual indicator showing available streak freezes with multiple display variants.
---

<ComponentPreview name="streak-freeze-indicator" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.trophy.so/r/streak-freeze-indicator.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="streak-freeze-indicator" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

Import the component:

```tsx
import { StreakFreezeIndicator } from "@/components/ui/streak-freeze-indicator"
```

Basic usage:

```tsx
<StreakFreezeIndicator freezes={2} maxFreezes={3} />
```

With Trophy API data:

```tsx
const streak = await trophy.streaks.get({ userId: "user_123" })

<StreakFreezeIndicator streak={streak} />
```

## Examples

### Variants

```tsx
<StreakFreezeIndicator freezes={2} maxFreezes={3} variant="dots" />
<StreakFreezeIndicator freezes={2} maxFreezes={3} variant="snowflakes" />
<StreakFreezeIndicator freezes={2} maxFreezes={3} variant="text" />
```

### Show Max

Display as "2/3" format.

```tsx
<StreakFreezeIndicator freezes={2} maxFreezes={3} showMax />
```

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `streak` | `StreakResponse` | - | Trophy API streak response |
| `freezes` | `number` | - | Manual freeze count |
| `maxFreezes` | `number` | - | Manual max freezes |
| `variant` | `"dots" \| "snowflakes" \| "text"` | `"dots"` | Display variant |
| `showMax` | `boolean` | `false` | Show "2/3" format |

### Types

```ts
interface StreakResponse {
  freezes?: number
  maxFreezes?: number
}
```

## Accessibility

- Uses `role="status"` for screen reader announcements
- Descriptive `aria-label` with freeze count
