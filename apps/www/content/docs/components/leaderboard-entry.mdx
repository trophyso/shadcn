---
title: Leaderboard Entry
description: A single leaderboard row component for displaying individual user rankings.
---

# Leaderboard Entry

A standalone component for rendering a single leaderboard row. Use it to build custom leaderboard layouts or display individual rankings.

<ComponentPreview name="leaderboard-entry" />

## Installation

```bash
npx shadcn@latest add https://ui.trophy.so/r/leaderboard-entry.json
```

## Usage

Import the component:

```tsx
import { LeaderboardEntry } from "@/components/ui/leaderboard-entry"
```

Basic usage:

```tsx
<LeaderboardEntry
  ranking={{ userId: "1", userName: "Alice", rank: 1, value: 1250 }}
/>
```

With Trophy API data:

```tsx
const leaderboard = await trophy.leaderboards.get({ leaderboardId: "weekly-xp" })

{leaderboard.rankings.map((ranking) => (
  <LeaderboardEntry key={ranking.userId} ranking={ranking} />
))}
```

## Examples

### Current User Highlight

Highlight the current user's entry.

<ComponentPreview name="leaderboard-entry-current-user" />

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `ranking` | `LeaderboardRanking` | Required | Ranking data |
| `isCurrentUser` | `boolean` | `false` | Highlight as current user |
| `isTopThree` | `boolean` | - | Force top-3 styling |
| `showAvatar` | `boolean` | `true` | Show avatar |
| `showValue` | `boolean` | `true` | Show value |
| `valueLabel` | `string` | - | Label for value |

### Types

```ts
interface LeaderboardRanking {
  userId: string
  userName: string | null
  rank: number
  value: number
}
```

## Accessibility

- Uses semantic HTML for ranking display
- Proper ARIA labels for screen readers
- Visual indicators for top positions
