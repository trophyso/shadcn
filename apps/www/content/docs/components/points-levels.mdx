---
title: Points Levels
description: A vertical levels timeline with optional sub-level details and progress tracking.
---

<ComponentPreview name="points-levels" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.trophy.so/r/points-levels.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="points-levels" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

Import the component:

```tsx
import { PointsLevels } from "@/components/ui/points-levels"
```

## Examples

### Basic Usage

<ComponentPreview name="points-levels" />

```tsx
<PointsLevels
  levels={[
    { id: "bronze", name: "Bronze", points: 75 },
    { id: "silver", name: "Silver", points: 150 },
    { id: "gold", name: "Gold", points: 275 },
  ]}
/>
```

### With Sub-levels

<ComponentPreview name="points-levels-sub-levels" />

```tsx
<PointsLevels
  levels={[
    {
      id: "bronze",
      name: "Bronze",
      points: 75,
      subLevels: [
        { name: "Bronze III", points: 75 },
        { name: "Bronze II", points: 100 },
        { name: "Bronze I", points: 125 },
      ],
    },
    {
      id: "silver",
      name: "Silver",
      points: 150,
      subLevels: [
        { name: "Silver III", points: 150 },
        { name: "Silver II", points: 190 },
        { name: "Silver I", points: 230 },
      ],
    },
  ]}
/>
```

### Progress Tracking

<ComponentPreview name="points-levels-progress-tracking" />

```tsx
<PointsLevels
  currentPoints={136}
  levels={[
    {
      id: "bronze",
      name: "Bronze",
      points: 75,
      subLevels: [
        { name: "Bronze III", points: 75 },
        { name: "Bronze II", points: 100 },
        { name: "Bronze I", points: 125 },
      ],
    },
    {
      id: "silver",
      name: "Silver",
      points: 150,
      subLevels: [
        { name: "Silver III", points: 150 },
        { name: "Silver II", points: 190 },
        { name: "Silver I", points: 230 },
      ],
    },
    { id: "gold", name: "Gold", points: 275 },
  ]}
/>
```

## With Trophy

Use the [Trophy SDK](https://docs.trophy.so/api-reference/client-libraries) to fetch user progress server-side:

<OpenApiCodeSamples
  spec="application.yml"
  method="get"
  endpoint="/users/{id}/points/{key}"
/>

Use the [Trophy SDK](https://docs.trophy.so/api-reference/client-libraries) to fetch points system level definitions server-side:

<OpenApiCodeSamples
  spec="application.yml"
  method="get"
  endpoint="/points/{key}/levels"
/>

Then aggregate both responses and pass them into `PointsLevels` props in your UI layer. Trophy does not support sub-levels yet.

```tsx
const currentPoints = userPointsResponse.total

const levels = (pointsLevelsResponse.levels ?? []).map((level) => ({
  ...level,
  subLevels: [],
}))

<PointsLevels levels={levels} currentPoints={currentPoints} />
```

## API Reference

### Props

| Prop                | Type                        | Default     | Description                                            |
| ------------------- | --------------------------- | ----------- | ------------------------------------------------------ |
| `levels`            | `PointsLevel[]`             | Required    | Vertical list of levels                                |
| `currentPoints`     | `number`                    | -           | User total used to derive current level and progress   |
| `currentLevelLabel` | `string`                    | `"Current"` | Label shown next to the current level                  |
| `formatPoints`      | `(value: number) => string` | -           | Optional formatter used when rendering computed ranges |

### Types

```ts
interface PointsSubLevel {
  name: string
  points: number
}

interface PointsLevel {
  id: string
  key?: string
  name: string
  description?: string
  badgeUrl?: string | null
  points: number
  subLevels?: PointsSubLevel[]
}
```
